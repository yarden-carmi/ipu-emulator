# Bazel configuration

# C/C++ standards
build --cxxopt=-std=c++17
build --conlyopt=-std=c11

# Warnings
build --copt=-Wall
build --copt=-Wextra
build --copt=-Wpedantic

# Coverage support
coverage --combined_report=lcov
coverage --experimental_generate_llvm_lcov
coverage --instrument_test_targets

# Test output
test --test_output=errors
test --test_summary=detailed

# Forward GITHUB_TOKEN into genrules that need it (e.g. generate_release_log)
build --action_env=GITHUB_TOKEN

# Launch any IpuApp harness in the TUI after setup, before instruction execution.
run:debug --run_env=IPU_DEBUG_TUI=1
